import styled from "styled-components/native";

import { ProfileScreenStyledProps as Props } from "./ProfileScreen.screen.types";

const ProfileScreenStyled = styled.View<Props>`
  flex: 1;
`;

export default ProfileScreenStyled;
